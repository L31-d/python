def factorial(n):
    if n==1:
        return 1 #边界
    else:
        return n*factorial(n-1)#递归函数

def F(x):
    if x==1:
        return x  #边界
    else:
        return (F(x-1)+1)*2 #递归函数
    print(F(n)) #输出递归值
